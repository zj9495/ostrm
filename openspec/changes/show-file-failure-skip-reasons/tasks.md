## 1. 后端结果模型

- [x] 1.1 新增文件处理结果对象，包含 `ProcessingResult` 状态和原因
- [x] 1.2 调整 `FileProcessorHandler` 接口返回文件处理结果对象
- [x] 1.3 更新所有 Handler 的 `SUCCESS` 返回点
- [x] 1.4 更新所有 Handler 的 `SKIPPED` 返回点，写入对应业务分支原因
- [x] 1.5 更新所有 Handler 的 `FAILED` 返回点，写入对应失败原因

## 2. 后端任务日志

- [x] 2.1 调整 `FileProcessorChain`，读取文件处理结果对象
- [x] 2.2 在处理链中为 `SKIPPED` 文件写入带文件路径、处理器和原因的任务运行日志
- [x] 2.3 在处理链中为 `FAILED` 文件写入带文件路径、处理器和原因的任务运行日志
- [x] 2.4 在 Handler 抛出异常时写入带文件路径、处理器和异常原因的任务运行日志
- [x] 2.5 确认不支持当前文件类型的 Handler 跳过不写入文件跳过日志

## 3. 前端运行日志

- [x] 3.1 检查运行日志弹窗布局，确保长文件路径和长原因可换行展示
- [x] 3.2 检查 `WARN` 与 `ERROR` 级别样式，确保跳过和失败原因易读

## 4. 验证

- [ ] 4.1 手动验证配置禁用导致跳过时，单次任务日志显示文件路径和跳过原因
- [ ] 4.2 手动验证本地目标文件已存在导致跳过时，单次任务日志显示文件路径和跳过原因
- [ ] 4.3 手动验证文件处理失败时，单次任务日志显示文件路径和失败原因
- [ ] 4.4 手动验证处理器不支持当前文件类型时，不产生文件跳过原因日志
