## 1. Restore documented commands

- [x] 1.1 Restore `install`.
- [x] 1.2 Restore `start-dev` and `up-dev`.
- [x] 1.3 Restore `stop`, `restart`, `build`, `rebuild`, `clean`, and `clean-all`.
- [x] 1.4 Restore `logs`, `logs -f`, and `logs-f`.
- [x] 1.5 Restore `status`, `exec`, `backup`, and `health`.

## 2. Verify script shape

- [x] 2.1 Run shell syntax validation for `dev-docker.sh`.
- [x] 2.2 Check the help output contains the restored commands.
- [x] 2.3 Confirm `dev-docker.sh` remains executable.
- [x] 2.4 Do not run Docker build or container startup unless explicitly requested.
