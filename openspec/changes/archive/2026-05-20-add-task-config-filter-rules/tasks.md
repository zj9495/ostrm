## 1. 后端数据模型

- [x] 1.1 新增 Flyway 迁移，为 `task_config` 添加过滤规则字段
- [x] 1.2 更新 `TaskConfig` 实体字段
- [x] 1.3 更新 `TaskConfigDto` 字段和 Bean Validation
- [x] 1.4 更新 `TaskConfigMapper.xml` 的 resultMap、字段列表、insert、update

## 2. 后端业务逻辑

- [x] 2.1 在 `TaskConfigService` 保存和更新时校验大小边界与正则表达式
- [x] 2.2 在 `FileDiscoveryHandler` 中按 `directoryNameExcludeRegex` 跳过目录递归
- [x] 2.3 在 `FileFilterHandler` 中实现任务配置过滤规则
- [x] 2.4 调整 `TaskExecutionService`，确保视频文件列表来自统一过滤逻辑
- [x] 2.5 增加过滤汇总日志，记录各规则过滤数量
- [x] 2.6 增加文件级过滤跳过日志，记录文件路径和具体跳过原因
- [x] 2.7 增加目录跳过日志，记录目录路径和具体跳过原因

## 3. 前端任务配置

- [x] 3.1 更新任务表单默认值和编辑回填
- [x] 3.2 增加文件大小过滤输入项，单位在 UI 中转换为 bytes 提交
- [x] 3.3 增加文件名排除正则输入项
- [x] 3.4 增加目录名称排除正则输入项
- [x] 3.5 在任务卡片展示已配置的过滤规则

## 4. 验证

- [ ] 4.1 手动验证现有任务未配置过滤规则时行为不变
- [ ] 4.2 手动验证文件大小下限过滤
- [ ] 4.3 手动验证文件名排除过滤
- [ ] 4.4 手动验证目录名称排除过滤
- [ ] 4.5 手动验证无效正则保存失败并返回明确错误
- [ ] 4.6 手动验证被过滤跳过的文件在任务日志中包含具体跳过原因
